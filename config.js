module.exports = {
  // Prefix untuk semua command, contoh: !menu, !ping
  prefix: "!",

  // Nama bot, ditampilkan di menu
  botName: "WA-Bot",

  // Nomor owner/admin bot (format: 62812xxxxxxx, tanpa +)
  ownerNumber: "628000000000",

  // Pakai pairing code (nomor HP) alih-alih scan QR
  // true  = login pakai kode pairing
  // false = login pakai scan QR di terminal
  usePairingCode: false,
};
